spring:
  datasource:
    url: jdbc:mysql://localhost:3306/MybatisTest?useSSL=false&serverTimezone=UTC
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml   # Mapper XML文件路径
  type-aliases-package: com.zsx.pojo  # 实体类包路径
